# Backend configuration — copy to .env and adjust for your setup.
# .env is gitignored; this file is the template.

HOST=127.0.0.1
PORT=8000

# Comma-separated list of allowed CORS origins (frontend dev server).
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Application metadata
APP_VERSION=0.1.0
